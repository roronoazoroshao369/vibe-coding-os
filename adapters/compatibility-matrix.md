# Adapter Compatibility Matrix

Use this matrix to choose the lightest setup for each assistant environment. The framework artifacts stay portable: commands are reusable prompts, skills are operating procedures, templates are project files, and memory should remain explicit and safe.

|| Tool | Primary instruction file | Commands support | Skills support | Skill format convention | Templates support | Memory workflow | Multi-agent workflow | Validation command | Known limitations | Recommended setup |
|| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|| Claude Code | `CLAUDE.md` | Strong: paste or invoke `commands/*.md` as phase prompts. | Strong: attach or reference `skills/*/*/SKILL.md` for task-specific procedures. | Attach skills via file reference or CLAUDE.md directive. Native subagent support for parallel skill execution. Context budget applies. | Strong: create specs, plans, tasks, reviews, and memory notes from `templates/*.md`. | Use `templates/memory-template.md` plus memory skills; keep durable decisions concise and never store secrets. | Use `skills/agents/*/SKILL.md` as role guides for architect, implementer, reviewer, and tester flows. | `npm run validate` | Native behavior depends on the active Claude Code workspace and attached context; avoid assuming every skill is always loaded. | Put `CLAUDE.md` at the project root, keep command prompts nearby, attach only the skills needed for the current task, and validate structural edits before final response. |
|| Codex | `AGENTS.md` | Strong: paste `commands/*.md` or use them as explicit workflow instructions. | Strong: use `skills/*/*/SKILL.md` as procedures when the task calls for a focused workflow. | Reference skill paths in AGENTS.md or paste content. Worker delegation available. Clear trigger descriptions in skills help Codex discover them by scope. | Strong: write normal repo files from `templates/*.md` so outputs are reviewable. | Use memory templates and memory skills as explicit files or summaries; avoid hidden context for durable project facts. | Use agent skills as role definitions or delegation checklists where the environment supports parallel agents. | `npm run validate` | Environment-specific final-response and citation rules may apply; sub-agent availability depends on the Codex runtime. | Keep `AGENTS.md` current, follow local repository instructions, use skills as scoped context, cite changed files when required, and commit only verified changes. |
|| Cursor | README/manual | Moderate: copy relevant `commands/*.md` content into chat or custom prompts. | Manual: reference or paste selected `skills/*/*/SKILL.md`; no guaranteed native skill loader. | Paste skill content into chat context per workflow phase. Self-contained skills work best. Compact skills (~500 words) respect Cursor context limits. | Strong: use `templates/*.md` as regular project documents. | Store durable context in project memory notes created from `templates/memory-template.md`; keep private data out of chat and files. | Manual: run role-based workflows with `skills/agents/*/SKILL.md` prompts in separate chats or review passes. | `npm run validate` | Cursor setup varies by workspace rules and chat mode; commands and skills are mostly manual unless configured by the user. | Start from the root README workflow, add concise workspace rules if useful, paste only the command or skill needed, and keep specs/plans/memory in versioned files. |
||| Gemini CLI | `GEMINI.md` or `AGENTS.md` | Moderate: copy relevant `commands/*.md` content into chat or session context. | Manual: reference or paste selected `skills/*/*/SKILL.md`; no guaranteed native skill loader. | Load skills at session start via GEMINI.md. Multi-turn sessions can reference skills across turns. Compact skills respect context shared with conversation history. | Strong: use `templates/*.md` as regular project documents. | Store durable context in project memory notes; keep private data out of chat and files. | Manual: run role-based workflows with separate prompts or review passes. | `npm run validate` | Gemini CLI loads instruction context at session start; point it at `GEMINI.md` or `AGENTS.md` and reference `commands/` and `skills/` by path. | Start from `adapters/gemini/README.md`, copy `AGENTS.md` as `GEMINI.md`, add concise session rules, paste only the command or skill needed, and keep specs/plans/memory in versioned files. |
||| Cline | `.clinerules` (+ optional `CLAUDE.md`) | Strong: paste `commands/*.md` as conversation prompts. Cline reads `.clinerules` as system context. | Strong: attach `skills/*/*/SKILL.md` via file reference. Supports `.cline/skills/` directory for native skill discovery. | Attach via file mention or skill directory. MCP servers can inject dynamic tools. Cline mode-specific rules (architect/ask/code) allow per-mode skill loading. | Strong: create project docs from `templates/*.md` as normal files. | Use `templates/memory-template.md` for durable decisions; Cline maintains per-session memory via MCP and `.clinerules`. | Strong: use subagent delegation. MCP tools provide extensible agent capabilities. Architect/ask/code modes align with role-based workflows. | `npm run validate` | MCP is the primary extensibility mechanism. `.clinerules` is read at startup and cannot change mid-session. Mode-specific rules provide excellent per-mode control. | Copy `AGENTS.md` as `.clinerules`, optionally create mode-specific rules (`.clinerules-architect`, `.clinerules-code`), reference command/skill files via chat, and validate via terminal. |
||| Continue.dev | `AGENTS.md` (+ `config.json` rules) | Strong: slash commands (`/spec`, `/plan`, `/implement`) reference Vibe command files. Define in `.continuerc.json`. | Strong: `AGENTS.md` per-directory rules provide skill context. Context providers (`@files`, `@codebase`) bring skill content into chat. | Define slash commands in config that reference skill paths. Context providers bring skill knowledge dynamically. Self-contained skills work best. | Strong: use `templates/*.md` as regular project files. Slash commands can create artifacts from templates. | AGENTS.md provides per-directory context. Memory content stored as project files. Continue injects AGENTS.md content automatically for the active directory. | AGENTS.md provides shared context across chat sessions. Multiple Continue instances can share workspace-level rules. | `npm run validate` | Setup varies between VS Code and JetBrains. Slash commands require config.json or .continuerc.json. Context providers are the most powerful way to bring in Vibe content. | Place `AGENTS.md` at project root, create `.continuerc.json` with slash commands referencing Vibe command files, use `@files` and `@codebase` for skill access, run validate via integrated terminal. |
||| Aider | `CONVENTIONS.md` (+ `.aider.conf.yml`) | Moderate: reference `commands/*.md` via `#` file prefix in chat, or paste command content. | Moderate: use `#` prefix to load skill files (`#skills/core/spec-first-development/SKILL.md`). No native skill loader. | Load skills via `#file` reference. Compact, self-contained skills work best. Aider reads `CONVENTIONS.md` at startup for persistent guidance. | Strong: create project files from `templates/*.md`. Aider auto-commits changes with conventional messages. | Store context in project files from templates. Aider's git-based approach makes all changes reviewable and revertable. | Architect/editor mode maps directly to Vibe's role-based workflow. Use `/architect` for spec/plan, `/code` for implementation. | `npm run validate` | Aider is terminal-only (no IDE). Best-in-class repo map via tree-sitter. Auto-commit behavior can be configured. Architect/editor mode is the ideal Vibe workflow surface. | Create `CONVENTIONS.md` with Vibe workflows, configure `.aider.conf.yml` with `lint: true` + `lint-command: "npm run validate"`, use `/architect` for planning and `/code` for implementation. |
||| Windsurf | `.windsurfrules` (falls back to `.cursorrules`) | Moderate: paste `commands/*.md` content into Cascade chat. Cascade reads `.windsurfrules` as system context. | Manual: reference `skills/*/*/SKILL.md` by pasting content. Cascade's Deep Context automatically surfaces relevant skill files. | Paste skill content into Cascade. Deep Context auto-discovers relevant files across workspace. Windsurf Flows provide reusable multi-step prompt chains. | Strong: create project docs from `templates/*.md`. Cascade edits files autonomously across the workspace. | Store durable context in project memory files. Cascade maintains session context via Deep Context analysis. | Windsurf's Cascade agent autonomously handles multi-file workflows. Flows automate sequenced multi-step tasks. | `npm run validate` | Windsurf is a closed-source IDE (Codeium). `.windsurfrules` is read at workspace load. Flows are the closest equivalent to Vibe skills — define them once and reuse. Legacy `.cursorrules` compatibility eases migration. | Copy `AGENTS.md` as `.windsurfrules`, use Cascade for autonomous implementation following Vibe workflows, create Flows for repeated multi-step sequences, and run validate via integrated terminal. |
|| Memory | `adapters/memory/README.md` plus project memory notes | N/A: memory adapters complement commands rather than invoking them directly. | Strong: use `skills/memory/*/SKILL.md` for capture, retrieval, handoff, and privacy review workflows. | N/A — memory adapters use skill content for procedure, not for session loading. | Strong: create durable notes from memory templates as explicit, reviewable files. | Primary purpose: local or external memory workflows; keep secrets out and prefer concise observations with sources. | Supports agent handoff by summarizing task state, decisions, and next steps for future sessions. | `npm run validate` | Memory adapters are optional and vary by storage backend; do not assume hidden or automatic recall across tools. | Start from `adapters/memory/README.md`, choose local/project-file memory by default, and use memory skills before storing or retrieving context. |

## Notes

- The validation command checks repository structure and reference metadata; use `npm run validate:references` only when changing reference-layer files.
- For all tools, prefer the smallest relevant command, skill, or template rather than loading the entire framework into every session.
- See [adapter install snippets](../docs/adapter-install-snippets.md) for copy-paste installation commands per tool.
