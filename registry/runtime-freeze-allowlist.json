{
  "schemaVersion": 1,
  "policy": "ADR 0002 freezes runtime scope. New runtime surface requires an ADR exception.",
  "allowedRuntimeTopLevelDirs": [
    "checkpoints",
    "core",
    "daemon",
    "install",
    "mcp",
    "memory",
    "sessions",
    "tasks",
    "teams"
  ],
  "allowedRuntimeScripts": [
    "runtime-audit.mjs",
    "runtime-behavior-tests.mjs",
    "runtime-checkpoint.mjs",
    "runtime-daemon.mjs",
    "runtime-events.mjs",
    "runtime-init.mjs",
    "runtime-install.mjs",
    "runtime-log.mjs",
    "runtime-mcp.mjs",
    "runtime-memory.mjs",
    "runtime-migrate.mjs",
    "runtime-replay.mjs",
    "runtime-session.mjs",
    "runtime-snapshot.mjs",
    "runtime-task.mjs",
    "runtime-team.mjs",
    "runtime-team-run.mjs",
    "runtime-validate.mjs"
  ],
  "allowedPackageScripts": [
    "runtime:init",
    "runtime:validate",
    "runtime:task",
    "runtime:memory",
    "runtime:checkpoint",
    "runtime:team",
    "runtime:session",
    "runtime:daemon",
    "runtime:mcp",
    "runtime:team-run",
    "runtime:install",
    "runtime:audit",
    "runtime:migrate",
    "runtime:replay",
    "runtime:snapshot",
    "runtime:events",
    "runtime:behavior-tests"
  ],
  "adr": "docs/adr/0002-runtime-scope-freeze.md",
  "adr_exception_template": "docs/adr/TEMPLATE-runtime-exception.md",
  "allowedRuntimeTopLevelDirs_rationale": {
    "checkpoints": "Optional runtime state \u2014 opt-in per ADR 0002.",
    "core": "Runtime core utilities \u2014 opt-in per ADR 0002.",
    "daemon": "Optional background process \u2014 opt-in per ADR 0002.",
    "install": "One-time installer \u2014 opt-in per ADR 0002.",
    "mcp": "Optional MCP server \u2014 opt-in per ADR 0002.",
    "memory": "Optional memory store \u2014 opt-in per ADR 0002.",
    "sessions": "Optional session state \u2014 opt-in per ADR 0002.",
    "tasks": "Optional task tracking \u2014 opt-in per ADR 0002.",
    "teams": "Optional team orchestration \u2014 opt-in per ADR 0002."
  }
}