# Master Plan v2.16.0 — Tổng hợp Hội đồng Chuyên gia

> Phiên họp: 2026-06-20
> Tình trạng: v2.15.0 SHIPPED, 33/33 gates PASS
> Quyết định: **DELAY Expert Mode → v2.17.0**, dùng v2.16.0 để fix 3 layers of gaps

## Tóm tắt từ 3 hội đồng

| Hội đồng | Verdict | Theme chính |
|----------|---------|-------------|
| **Security & Trust** | **B** | Foundation vững nhưng ADR 0004 chưa wire code, RTL-002 attack còn tồn tại, 0 tests cho security commands |
| **Engineering** | **B+** (8.0/10) | Governance mature, behavior under-verified (4.6% test coverage) |
| **Adoption** | **B+** | Adapter docs không đồng đều, SECURITY.md mỏng, 132 skills thiếu example |

**Consensus: TẤT CẢ 3 hội đồng đề xuất DELAY Expert Mode**, dùng v2.16.0 để close gaps.

## Lý do delay Expert Mode

1. **Security**: ADR 0004 (Adaptive Trust Levels) chỉ document-only. Wiring nó vào code cần 2-3 sprints, không nên thêm Expert Mode attack surface.
2. **Engineering**: Quality gate warning-only, test coverage 4.6% — Expert Mode tăng complexity mà verification yếu.
3. **Adoption**: 4 gaps critical (compatibility-matrix, SECURITY.md, memory adapter, 132 examples) — block enterprise adoption.

## v2.16.0 Theme: "Close the Gaps"

### Wave A — Security Wiring (priority: CRITICAL)
1. **Wire Layer 0 Trust Scoring** — tạo `security/defense/trust-scorer.mjs` (ADR 0004:73-95)
2. **Fix RTL-002 false negative** — mở rộng regex U+202A–U+202E + U+2066–U+2069 + homoglyphs; thêm 5+ canary payloads
3. **Implement `vibe-bypass-detect` script** — chuyển từ doc-only sang executable
4. **Tests cho 3 security commands** — bypass-detect, adversarial-detect, license-surface (0% → 100% coverage)
5. **Resolve 4 TODO ở `scripts/test-generator.mjs`** — implement hoặc xóa

### Wave B — Quality Hardening (priority: HIGH)
1. **Quality gate: WARNING → BLOCKING** cho non-falsifiable items
2. **Test coverage 4.6% → ≥15%** — implement property-test-runner
3. **Consolidate `skills/core/` 88 → ~30** (umbrella + references/)
4. **Parallelize `validate:all`** 34s → ~12s
5. **De-stub 5 adapter** — install.sh thực hoặc `status: documentation-only`

### Wave C — Adoption Polish (priority: HIGH)
1. **`adapters/compatibility-matrix.md`** — 6 adapter × capability matrix
2. **`scripts/generate-example-skeleton.mjs`** — bulk gen examples, 132→50 gap
3. **Expand `SECURITY.md` 18 → 80+ dòng** (supported versions, SLA, severity matrix)
4. **Expand `adapters/memory/README.md` 36→100+ dòng** + example
5. **RFC template + `docs/rfcs/0001-expert-mode.md`** — prepare for v2.17.0

## Validation criteria
- 33/33 gates PASS
- New gates: trust-scorer-wired, rtl-coverage-100, security-command-coverage-100, quality-gate-blocking
- Target: 35+ gates total
- Test coverage ≥15% (5/152 → 23/152)
- 0 open TODO/FIXME trong shipped code

## Post-ship: v2.17.0 — Expert Mode
Sau khi v2.16.0 fix gaps, v2.17.0 sẽ:
1. Task risk classifier
2. Model-aware adapter
3. Auto-scope decision
4. Compact failure-mode library
5. Adaptive prompt escalation
6. Critic pass
