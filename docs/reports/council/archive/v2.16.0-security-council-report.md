# Hội đồng Chuyên gia Security & Trust — v2.15.0

## 1. Current Security Posture
Nền tảng vững chắc. ADR 0003 (3-layer Defense in Depth) implement đầy đủ:
- **Layer 1 DETECT**: `security/defense/injection-counters.mjs` — **31 patterns thực tế** (không phải 19 như brief). Coverage: 37/38 = 97.37% (`tests/security/baseline.json`).
- **Layer 2 CONTAIN**: `security/redact/redactor.mjs` + 30 patterns trong policy. FP rate 0%.
- **Layer 3 RECOVER**: `tests/security/regression.mjs` chạy 43 canary payloads, baseline locked.
- **5 hook refactors** wired (ADR 0004). **Sandbox-marker heuristic**: 11 patterns (WebFetch, fetch, axios, ...).

## 2. Gaps & Risks
| # | Gap | Vị trí | Mức độ |
|---|-----|--------|--------|
| 1 | RTL override (unicode-002) attack | `injection-counters.mjs:126` regex U+202A–U+202E không đầy đủ | **HIGH** |
| 2 | 4 files có TODO/FIXME thực sự | `scripts/test-generator.mjs:344,355,365,417` (4×), `validate-quality-diff.mjs:16,29`, `property-test-runner.mjs:129,343` | MEDIUM |
| 3 | Sandbox-marker chỉ 11 patterns (không phải 12) — thiếu `wget`, `nc`, `ssh` | `validate-sandbox-marker.mjs:25` | MEDIUM |
| 4 | License policy: 21 `import_mode: inspiration` (1 thiếu LICENSE upstream) | `references/index.json:437` (forrestchang) | MEDIUM |
| 5 | **`vibe-bypass-detect` chỉ là doc, không có script** | `commands/vibe-bypass-detect.md` | **HIGH** |

## 3. Test Coverage
- 7 test files (2 hook + 5 skill regression)
- **0 tests cho 3 security commands** (bypass-detect, adversarial-detect, license-surface)
- Coverage security commands = 0/3 = 0%

## 4. Adversarial Readiness
**ADR 0004 (Adaptive Trust Levels) CHƯA wire code** — `grep 'trust_class|loop_detect|license_class' security/ scripts/` → 0 results. Compliance matrix claim "v2.15.0: ✓" nhưng code không tồn tại. Đây là "documented but not implemented".

## 5. Recommendations cho v2.16.0
1. **[CRITICAL] Wire Layer 0 Trust Scoring vào code** — tạo `security/defense/trust-scorer.mjs` implement theo ADR 0004:73-95
2. **[CRITICAL] Fix RTL-002 false negative** — mở rộng regex U+202A–U+202E + U+2066–U+2069 + homoglyphs; thêm 5+ canary payloads
3. **[HIGH] Thêm tests cho 3 security commands** (bypass-detect, adversarial-detect, license-surface)
4. **[HIGH] Resolve 4 TODO ở `scripts/test-generator.mjs`** — implement hoặc xóa
5. **[MEDIUM] Mở rộng sandbox-marker** từ 11 → ≥18 patterns

## Verdict: **B** (Good foundation, incomplete wiring)
