# vibe-reference-index

Rebuild and check the reference index. Ensure every source doc has an index entry, every index entry has a changelog, mappings mention each source, relevant feature docs are linked, local target paths exist, and JSON is valid. Run `npm run references:index` when you need to refresh `last_built`, then run `npm run validate:references`.
