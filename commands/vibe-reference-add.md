# vibe-reference-add

Add a new reference repository to the Reference Intelligence Layer. Create `references/sources/<id>.md`, create `references/changelogs/<id>.md`, add the source to `references/index.json`, update mapping docs, identify relevant feature links, and update attribution files only if material is imported or closely adapted. Do not copy upstream content. Run `npm run validate:references` and report assumptions.
