# vibe-doctor

Diagnose repository health. Check required files, registries, skills, commands, templates, package scripts, and obvious documentation gaps. Run `npm run validate` when available and report concrete fixes.
