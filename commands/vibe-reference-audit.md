# vibe-reference-audit

Audit one or more tracked references. Read `references/index.json`, the source docs, feature docs, and mappings; inspect the upstream repository; summarize relevant changes in original language; update the local changelog; identify impacted local files; propose or implement improvements only when useful; update `last_checked` and `last_known_commit` when known. Do not blindly merge or copy upstream content. Run validation.
