# Model Config Output

> Generated by `vibe-model-config` — `{{generated_at}}`

## Model Info

| Field | Value |
| ----- | ----- |
| **Model ID** | `{{model_id}}` |
| **Vendor** | `{{vendor}}` |
| **Name** | `{{name}}` |
| **Capability** | `{{capability}}` |
| **Default Quality Stack** | `{{default_quality_stack}}` |
| **Advisory Level** | `{{advisory_level}}` |

## Task Context

- **Task Type:** `{{task_type}}`
- **Task Risk:** `{{task_risk}}`
- **Minimum Quality Stack:** `{{min_quality_stack}}`
- **Stack / Domain Signals:** `{{stack_signals}}`

## Effective Quality Stack

> **{{effective_quality_stack}}**

{{#if escalation_note}}
> ⚠ Escalation note: `{{escalation_note}}`
{{/if}}

{{#if override_note}}
> ⚠ Override note: `{{override_note}}`
{{/if}}

## Recommended Quality Packs

| # | Pack | Source | Path |
| - | ---- | ------ | ---- |
{{#packs}}
| {{index}} | {{name}} | {{source}} | `{{path}}` |
{{/packs}}

## Required Gates (from task-risk profile)

- {{#required_gates}}`{{.}}`{{#unless @last}}, {{/unless}}{{/required_gates}}

## Model Weakness Notes

{{#if weakness_matches}}
{{#weakness_matches}}
| Model | Pattern | Prevention Check |
| ----- | ------- | ---------------- |
| `{{model}}` | `{{pattern}}` | `{{prevention_check}}` |
{{/weakness_matches}}
{{else}}
No weakness matches found for `{{model_id}}` in the current weakness log.
{{/if}}

## Warning Notes

{{#if warning_notes}}
{{#warning_notes}}
- [{{severity}}] {{message}}
{{/warning_notes}}
{{else}}
None.
{{/if}}

## Tailored Checklist

1. **[Required] {{#required_gates}}`{{.}}`{{#unless @last}}, {{/unless}}{{/required_gates}}** — verify required gates from task-risk profile pass before proceeding.
2. **Confirm effective quality stack (`{{effective_quality_stack}}`)** — not downgraded below `{{min_quality_stack}}` without explicit approved override.
3. **Run recommended quality packs** — load each pack from the Recommended Quality Packs table and execute.
4. **Apply injected weakness checks** — {{#if weakness_matches}}the {{weakness_matches.length}} matched patterns above are injected as mandatory pre-flight checks{{else}}no model-specific weakness matches to inject{{/if}}.
5. **Review advisory level (`{{advisory_level}}`)** — obey advisory thoroughness: `minimal` shows only critical, `normal` shows all, `thorough` includes deep analysis.
6. **Post-task weakness log update** — if a new weakness is observed, add it to `templates/model-weakness-log.md` with evidence.

---
*Generated by `vibe-model-config`. Hand off to Quality Engine or manual review.*
